name: GitHub App Dummy Execution Test

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  dummy:
    runs-on: ubuntu-latest

    steps:
      - name: Print execution context
        run: |
          echo "Workflow triggered successfully"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Actor: $GITHUB_ACTOR"
          echo "Run ID: $GITHUB_RUN_ID"

      - name: Simulate work
        run: |
          echo "Doing nothing..."
          sleep 10
          echo "Dummy job completed"
